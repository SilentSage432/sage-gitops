apiVersion: v1
kind: Service
metadata:
  name: lambda-api
  namespace: arc-lambda-locked
spec:
  selector: { app: lambda-agent }
  ports: [ { name: http, port: 8000, targetPort: 8000, protocol: TCP } ]
