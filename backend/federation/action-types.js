// Supported action types for the federation action plane.
// This list will expand as the system matures.

export const ACTION_TYPES = [
  "node.ping",
  "node.status",
  "config.update",
  // Add more action types as needed
];

