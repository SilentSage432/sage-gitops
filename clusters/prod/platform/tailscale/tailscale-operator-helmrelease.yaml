apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  interval: 10m
  chart:
    spec:
      chart: tailscale-operator
      sourceRef:
        kind: HelmRepository
        name: tailscale
        namespace: flux-system
      version: 1.x.x
  values:
    # <-- put the fresh OAuth client from the Tailscale admin here -->
    oauth:
      clientId: kE28RkVJpX11CNTRL      # e.g. 3C3C8D3B... (not tskey-…)
      clientSecret: tskey-client-kE28RkVJpX11CNTRL-eMkU3ipTZGY2megicTn4GYKWM4rMfJkCB

    # keep tags simple; operator earns k8s-operator and can assign 'k8s'
    operatorConfig:
      defaultTags: "tag:k8s-operator"
    proxyConfig:
      defaultProxyClass: default
      defaultTags: "tag:k8s"
