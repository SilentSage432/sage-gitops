apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  interval: 10m
  chart:
    spec:
      chart: tailscale-operator
      sourceRef:
        kind: HelmRepository
        name: tailscale
        namespace: flux-system
      version: 1.x.x
  values:
    # Let the chart create the Secret for OAuth
    oauth:
      # Use the exact values from Tailscale admin
      clientId: PFBVVF9ZT1VSX1RBSUxTQ0FMRV9PQVVUSF9DTElFTlRfSURfSEVSRT4=
      clientSecret: PFBVVF9ZT1VSX1RBSUxTQ0FMRV9PQVVUSF9DTElFTlRfU0VDUkVUX0hFUkU+

    # Existing auth key for the operator to join
    authKey:
      secretName: tailscale-auth
      secretKey: TS_AUTHKEY

    operatorConfig:
      logging: info

    proxyConfig:
      defaultProxyClass: default
      # Must be a STRING for 1.88.x
