# Security: Never commit sensitive files
*.yaml.bak
*.backup.*
mc.yaml
mc.backup.*
mc.*.yaml

# Cloudflare tokens and secrets
**/cloudflared*.yaml
**/cloudflare*.yaml

# Talos machine configurations with secrets
**/machineconfig*.yaml
**/talos*.yaml

# Certificates and private keys
*.crt
*.key
*.pem
*.p12
*.pfx

# Age encryption keys (SOPS)
age.key
.sops.pubkey
**/age.key
**/.sops.pubkey

# Environment files with secrets
.env
.env.local
.env.production

# Configuration files that may contain secrets
config/*.yaml
# Allow encrypted secrets but not plaintext ones
secrets/*.yaml
!secrets/*.enc.yaml
**/secrets/**
!**/secrets/*.enc.yaml
**/config/**

# Tailscale and other service secrets
**/auth-secret.yaml
**/operator-helmrelease.yaml
**/tailscale-operator-helmrelease.yaml
**/*-secret.yaml
**/*-auth.yaml

# Temporary files
*.tmp
*.temp
*~