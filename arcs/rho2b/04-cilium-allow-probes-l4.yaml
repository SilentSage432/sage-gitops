apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: rho2b-allow-node-probes-l4
  namespace: arc-rho2b
spec:
  endpointSelector:
    matchLabels:
      app: rho2b-api
  ingress:
    - fromEntities: ["host","remote-node","kube-apiserver","cluster"]
      toPorts:
        - ports:
            - port: "8000"
              protocol: TCP
