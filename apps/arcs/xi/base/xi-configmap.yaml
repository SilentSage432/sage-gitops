apiVersion: v1
kind: ConfigMap
metadata:
  name: xi-policy
  namespace: arc-xi
data:
  retention.yaml: |
    segment_max_mb: 64
    keep_segments: 120
    subjects:
      - omega.reason
      - sigma.telemetry.>
      - lambda.acks.>
